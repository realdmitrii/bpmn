/**
 * Snap during resize.
 *
 */
export default class ResizeSnapping {
    static $inject: string[];
    /**
     * @param eventBus
     * @param snapping
     */
    constructor(eventBus: EventBus, snapping: Snapping);
    initSnap(event: any): any;
    addSnapTargetPoints(snapPoints: any, shape: any, target: any, direction: any): any;
    getSnapTargets(shape: any, target: any): (import("diagram-js/lib/model/Types").ShapeLike | import("diagram-js/lib/model/Types").ConnectionLike)[];
}

type EventBus = import('diagram-js/lib/core/EventBus').default;
type Snapping = import('diagram-js/lib/features/snapping/Snapping').default;
